'use client';

export default function History() {
    // This page is where we can access the history of the treatments
    // if one of the treatments list is clicked, it should redirect to the treatment page
    // with the data of that treatment
    return (
        <div>
            <h1 className="text-4xl text-foreground">HISTORY</h1>
        </div>
    );
}
