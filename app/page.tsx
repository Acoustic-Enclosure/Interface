'use client';

export default function Home() {

  // This page is where its destined te be data related to the last tuning
  // should be a component and in history one can access that component too but with the 
  // data of different treatments
  return (
    <div>
      <h1 className="text-4xl text-foreground">HOME</h1>
    </div>
  );
};
